/** @type {import('tailwindcss').Config} */
module.exports = {
  purge: ["./**/*.html"], // This will enable purging of unused styles for all .html files in the current directory and its subdirectories
  theme: {
    extend: {},
  },
  plugins: [],
};
